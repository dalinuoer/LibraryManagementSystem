#include <iostream>
#include <fstream>
#include "User.h"
#include "Book.h"
#include "Record.h"
#include "consoleCommand.h"

using namespace std;

int main()
{
	/*User user;

	cout << sizeof(User) << endl;
	cout << sizeof(user) << endl;

	user.setName("ÇúÉÙÎ°");

	cout << user.getName() << endl;

	ofstream out;
	out.open("test.dat");
	out.write((char*)&user, sizeof(user));
	out.flush();
	out.close();*/
	return 0;
}